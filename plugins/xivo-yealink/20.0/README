Warning: to install firmware for this plugin, the 'unrar' executable must be
present on the host system. If it is not, you'll have a runtime error while
trying to install any firmware.


== Installing dependencies on Debian Squeeze ==

You must have the 'non-free' repository added in /etc/apt/sources.list

$ apt-get update
$ apt-get install unrar
